# Pre-August baseline migrations

These SQL files are historical migrations from the pre-August project lifecycle.
They are preserved for audit and repository history only.

The active Supabase migration chain starts at
`20260816120000_create_accounts_schema.sql`. Do not move these files back into
`supabase/migrations` and do not replay them against the current remote project.
