import { AlertTriangle, Landmark } from "lucide-react"
import { useTranslation } from "@/i18n/useTranslation"

export function AccountWorkspaceSkeleton() { return <div aria-busy="true" aria-label="Loading accounts" className="animate-pulse space-y-7 motion-reduce:animate-none"><div className="h-32 border-b bg-muted/30" /><div className="h-12 bg-muted/30" /><div className="space-y-3">{Array.from({ length: 6 }, (_, index) => <div key={index} className="h-16 border-y bg-muted/20" />)}</div></div> }
export function AccountWorkspaceError({ error, onRetry }: { error: Error; onRetry: () => void }) { const { t } = useTranslation(); return <div role="alert" className="flex min-h-80 flex-col items-center justify-center text-center"><AlertTriangle className="text-red-600" /><h1 className="mt-4 text-xl font-semibold">{t("accounts.error.loadTitle")}</h1><p className="mt-2 max-w-md text-sm text-muted-foreground">{error.message}</p><button type="button" onClick={onRetry} className="tharwati-button-primary mt-5">{t("accounts.actions.tryAgain")}</button></div> }
export function AccountWorkspaceEmpty({ filtered, onClear }: { filtered: boolean; onClear: () => void }) { const { t } = useTranslation(); return <div className="flex min-h-64 flex-col items-center justify-center text-center"><Landmark className="text-muted-foreground" /><h2 className="mt-4 text-lg font-semibold">{t(filtered ? "accounts.workspace.emptyFiltered" : "accounts.empty.title")}</h2><p className="mt-2 max-w-md text-sm text-muted-foreground">{t(filtered ? "accounts.workspace.emptyFilteredDescription" : "accounts.empty.description")}</p>{filtered ? <button type="button" onClick={onClear} className="mt-4 text-sm font-semibold text-primary hover:underline focus-visible:ring-2">{t("assets.workspace.clearFilters")}</button> : null}</div> }
