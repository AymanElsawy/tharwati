import type { AccountDetailEvidence } from "@/features/accounts/types/account-workspace"
import { useTranslation } from "@/i18n/useTranslation"

export function AccountEvidenceRelationships({ detail }: { detail: AccountDetailEvidence | null }) { const { t } = useTranslation(); return <section aria-labelledby="account-evidence-title" className="mt-14 border-t border-[var(--border-subtle)] pt-9"><header><p className="tharwati-eyebrow">{t("accounts.evidence.eyebrow")}</p><h2 id="account-evidence-title" className="tharwati-section-title mt-2">{t("accounts.evidence.title")}</h2><p className="tharwati-section-description">{detail ? t("accounts.evidence.selected", { name: detail.item.account.name }) : t("accounts.evidence.prompt")}</p></header>{detail ? <dl className="mt-6 grid gap-px border-y border-[var(--border-subtle)] bg-[var(--border-subtle)] sm:grid-cols-3"><Evidence label={t("accounts.workspace.holdings")} value={detail.holdings.length}/><Evidence label={t("accounts.relationships.linked_assets")} value={detail.assets.length}/><Evidence label={t("accounts.evidence.postedActivity")} value={detail.activity.length}/></dl> : null}</section> }
function Evidence({ label, value }: { label: string; value: number }) { return <div className="bg-background px-5 py-5"><dt className="text-xs text-muted-foreground">{label}</dt><dd className="mt-2 text-xl font-semibold tabular-nums">{value}</dd></div> }
